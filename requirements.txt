# Core
flask>=2.3.0
flask-cors>=4.0.0
PyJWT>=2.8.0

# Database
# (uses stdlib sqlite3 — no pip install needed)

# Embeddings
sentence-transformers>=2.2.0
numpy>=1.24.0

# ─── Dev / Testing ───────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-flask>=1.3.0
